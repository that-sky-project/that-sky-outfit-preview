# Sky 衣柜 · 1Panel 部署教程

> 部署对象：`SkyWardrobe-site.zip`（80MB 纯静态站，无需 Node/PHP/数据库）
> 访客效果：打开网址即看到 3D 角色 + 完整衣柜换装，**无需导入 APK**
> 部署方式：1Panel 创建「静态网站」→ 上传解压 → 完成

---

## 0. 准备

| 需要 | 说明 |
|---|---|
| 一台 Linux 服务器 | 云服务器（阿里云/腾讯云/AWS…）或家宽主机均可 |
| 1Panel | 未安装的看第 1 节，已安装的跳过 |
| 部署包 | 本机 `SkyWardrobe-site.zip`（80MB） |
| 域名（可选） | 没有域名也能用 `http://服务器IP:端口` 直接访问 |

---

## 1. 安装 1Panel（如未安装）

SSH 登录服务器，执行官方一键安装脚本：

```bash
curl -sSL https://resource.fit2cloud.com/1panel/package/quick_start.sh -o quick_start.sh
sudo bash quick_start.sh
```

按提示选择安装路径、端口、面板账号密码。装完后浏览器打开 `http://服务器IP:面板端口` 登录。
（面板默认端口通常是 `10496` 等随机端口，安装完成时会打印出来。）

---

## 2. 上传部署包（二选一）

### 方式 A：1Panel 网页文件管理器（最简单）

1. 1Panel → **文件** → 进入站点目录（还没建站可先到 `/opt/1panel/www/`，见第 3 节建站后再传进站点目录）
2. 点 **上传**，选择本机 `SkyWardrobe-site.zip`
   - 80MB 浏览器上传可能要几分钟，进度条走完前别关页面
3. 上传完成后，右键该 zip → **解压**（或选中后点「解压」）

### 方式 B：SFTP / scp（快、稳，推荐大文件）

本机命令行（Windows 可用 Git Bash / PowerShell）：

```bash
scp "D:\SkyWardrobe-site.zip" root@服务器IP:/opt/1panel/www/
```

或用 FileZilla/WinSCP：主机=服务器IP，端口=22，账号=服务器系统用户，把 zip 拖进 `/opt/1panel/www/`，再 SSH 进去解压：

```bash
cd /opt/1panel/www
unzip SkyWardrobe-site.zip
```

---

## 3. 创建静态网站

1Panel → **网站** → **创建网站** → **静态网站**

| 配置项 | 填写 |
|---|---|
| 主域名 | 填你的域名；没有域名就填 `IP:端口`，如 `123.45.67.89:8080`（先放行该端口，见第 6 节） |
| 主目录 | 默认自动生成，如 `/opt/1panel/www/sites/你的域名`（记下这个路径） |
| 其他 | 全部默认 |

点 **确认**。

---

## 4. 把文件放进站点根目录

关键：确保站点根目录下**直接**是这些内容：

```
站点根目录/
├─ index.html        ← 入口，必须在根目录
├─ js/               （8 个文件，站点逻辑）
├─ data/             （物品定义 + 贴图）
├─ meshes/           （1029 个模型）
└─ icons/            （1252 个图标）
```

- 若解压后出现多一层 `SkyWardrobe/` 文件夹，把里面所有内容**移到**站点根目录（或把网站的主目录改指向 `SkyWardrobe/` 那一层）。
- 可在 1Panel 文件管理器里直接检查目录结构。

---

## 5. 访问验证

浏览器打开：

```
http://你的域名          （有域名）
http://服务器IP:端口      （无域名）
```

看到 **“Sky 衣柜 - 换装间”** 页面、左侧 3D 角色、右侧衣柜面板即部署成功。

---

## 6. 防火墙 / 安全组放行（打不开时先查这里）

- **云服务器控制台 → 安全组**：放行 `80`（HTTP）和 `443`（HTTPS），自定义端口要额外放行该端口
- **服务器内防火墙**（如有 ufw / firewalld）：

```bash
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 8080/tcp   # 自定义端口按实际改
```

- 1Panel → **安全** → **防火墙**：同样放行以上端口

---

## 7. 配置 HTTPS（可选但推荐）

1Panel → **网站** → 选中该站点 → **网站 → SSL** → 选择证书类型（Let's Encrypt 免费证书）→ **申请**
申请成功后开启 **HTTPS**，建议勾选「强制 HTTPS」。

---

## 8. 常见问题

| 现象 | 原因 / 解决 |
|---|---|
| 打不开、超时 | 安全组/防火墙没放行端口（见第 6 节） |
| 404 白页 | 文件没放对：`index.html` 必须在站点根目录 |
| 图标全裂 / 模型不加载 | `js / data / meshes / icons` 目录不完整；重新解压完整 zip（约 80MB） |
| 页面是旧版 | 浏览器缓存，`Ctrl + F5` 强刷 |
| 首次打开模型加载慢 | 1029 个模型按需加载，切分类时会有 1~3 秒加载，属正常 |
| 网页上传 80MB 太慢 | 改用 SFTP/scp（方式 B） |

---

## 9. 数据与版权说明

- 图标：来自 vithai.xyz 官方图集（UIPackedAtlas 1–41），与线上对照站逐像素一致
- 模型/数据：光遇 Steam 客户端与官方 APK 解包，仅供个人学习与欣赏
- 站内无任何联网请求，全部资源本地加载
